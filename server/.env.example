# Environment Variables for Production
# COPY THIS FILE TO .env AND UPDATE THE VALUES

# Server Configuration
PORT=3000
NODE_ENV=production

# Security
# Generate a strong random secret key for production
# You can generate one with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
SECRET_KEY=CHANGE_THIS_TO_A_RANDOM_64_CHARACTER_HEX_STRING_IN_PRODUCTION

# CORS Settings (frontend URLs allowed to access the API)
# Add your production frontend URL here
ALLOWED_ORIGINS=http://localhost:5502,http://127.0.0.1:5502,https://your-frontend-url.netlify.app

# Database (if upgrading from JSON files)
# DATABASE_URL=mongodb://...
# or
# DATABASE_URL=postgresql://...
